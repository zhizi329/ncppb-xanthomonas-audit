# Raw Data

Put original, unedited input files here.

Recommended files:

```text
ncppbresult.html       saved HTML from the NCPPB Xanthomonas result page
ncppb_catalogue.csv    exported or manually cleaned NCPPB table if HTML extraction is difficult
```

Do not manually edit raw files after saving them. Create cleaned versions in `data/processed/` instead.
